services:
  api:
    build: .
    container_name: mini-motor-notificacoes
    ports:
      - "8000:8000"
    volumes:
      # Reflete alterações do código no container (hot reload em desenvolvimento)
      - ./:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
